# Local demonstration values only. Do not use these credentials in production.
POSTGRES_DB=testozon
POSTGRES_USER=testozon
POSTGRES_PASSWORD=testozon-local-only

# Host ports used by the two independent Compose profiles.
MEMORY_HTTP_HOST=127.0.0.1
MEMORY_HTTP_PORT=8080
POSTGRES_HTTP_HOST=127.0.0.1
POSTGRES_HTTP_PORT=8081
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
